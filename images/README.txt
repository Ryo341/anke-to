Place character image files in this folder.

Required file names:
- chara-left.png
- chara-center.png
- chara-right.png

Notes:
- Transparent PNG is recommended.
- If you keep these file names, `index.html` loads them automatically.
- To hide a slot, set that `url` to `""` in `CHARACTER_IMAGES`.
